import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['192.168.29.154', 'unwanton-forgivingly-hoyt.ngrok-free.dev'],
};

export default nextConfig;
